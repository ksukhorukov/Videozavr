<%= form_for(@video) do |f| %>
  <%= render 'shared/error_messages', object: f.object %>
  <div class="field">
    <%= f.text_field :watermark, placeholder: "Watermark..." %>
    <%= f.file_field :movie %>
  </div>
  <%= f.submit "Post", class: "btn btn-large btn-primary" %>
<% end %>